import React from "react";
import '../Content.scss';

function RemovePane({ botInfo }) {
    return <div id="remove-pane">
        {/* <div id="invite-box">
            <h2>Invite Bot to Server</h2>
            <img src={`https://cdn.discordapp.com/avatars/${botInfo.id}/${botInfo.avatar}.png`} alt=""/>
            <a href={invite} target="_blank" rel="noopener noreferrer">Invite Me</a>
        </div> */}
    </div>;
}

export default RemovePane;